export default function ChatThreadPage({ params }: { params: { id: string } }) {
    // The layout handles all the chat functionality based on the URL
    // This component is just a placeholder that gets rendered inside the layout
    return null;
}